[package]
name = "rust"
version = "0.1.0"
authors = ["Rodrigo Freitas"]

[lib]
name = "plugin"
crate-type = ["dylib"]

[dependencies]
rcollections = { path = "../../../rust/rcollections", version = "0.1" }
libc = "0.2.0"
